<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="account_move_inherit" model="ir.ui.view">
            <field name="name">account_move.form</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="priority" eval="8"/>
            <field name="arch" type="xml">
                <xpath expr="//page[@name='invoice_tab']//field[@name='invoice_line_ids']/tree/field[@name='price_total']" position="after">
                    <field name="main_line" invisible="1"/>
                    <field name="persons"/>
                    <field name="nights"/>
                    <field name="children"/>
                </xpath>
                <xpath expr="//field[@name='partner_id']" position="after">
                    <field name="payment_id"/>
                    <field name="payment_ids"/>
                    <field name="cruise_id"/>
                </xpath>

            </field>
        </record>
    </data>
</odoo>